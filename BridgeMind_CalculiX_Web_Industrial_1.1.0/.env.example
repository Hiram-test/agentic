# 设置网站映射到主机的 HTTP 端口。
BRIDGEMIND_HTTP_PORT=80
# 设置一个或多个逗号分隔的高强度 API 密钥；留空仅适合本地受信网络。
BRIDGEMIND_API_KEYS=replace-with-a-long-random-secret
# 约束 OpenMP 线程数并避免服务器过载。
OMP_NUM_THREADS=4
# 约束 CalculiX 结果处理线程数。
CCX_NPROC_RESULTS=4
