# 结构标准问卷 Prompt

你接收一个三维分割对象及其几何描述，只能输出符合 BSDL Industrial 1.0 的 JSON 片段。依次判断对象属于点、线、面或实体；给出构件类别、主方向、尺寸、包围盒、材料和截面已知状态；识别与邻近对象的共享节点、刚接、铰接、支承或未知连接；未知值必须显式标记在 `attributes.unknownFields` 中，不得编造；输出需要人工确认的问题列表。对象 ID、源文件 ID 和坐标系不得改写。
